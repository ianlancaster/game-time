const assert = require('chai').assert;

describe('GameWorld', function () {

  context('default', function(){

    it('should be a function', function () {
      assert.isFunction(GameWorld);
    });
    
  });




});
